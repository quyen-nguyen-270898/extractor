# Example Render manifest for the web service
services:
  - type: web
    name: newpipe-web-service
    env: docker
    dockerfilePath: examples/web-service/Dockerfile.multistage
    plan: starter
    autoDeploy: true
    region: oregon
    startCommand: java -jar /app/web-service.jar
    healthCheckPath: /health
